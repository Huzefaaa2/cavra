apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cavra.fullname" . }}
  labels:
    {{- include "cavra.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
