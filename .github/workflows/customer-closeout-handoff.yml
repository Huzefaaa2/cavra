name: Customer Closeout Handoff

on:
  pull_request:
    paths:
      - "src/cavra/customer_closeout_handoff.py"
      - "scripts/validate_customer_closeout_handoff.py"
      - "examples/customer-closeout-handoff/**"
      - "tests/test_customer_closeout_handoff.py"
      - ".github/workflows/customer-closeout-handoff.yml"
  push:
    branches:
      - main
    paths:
      - "src/cavra/customer_closeout_handoff.py"
      - "scripts/validate_customer_closeout_handoff.py"
      - "examples/customer-closeout-handoff/**"
      - "tests/test_customer_closeout_handoff.py"
      - ".github/workflows/customer-closeout-handoff.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CAVRA
        run: python -m pip install -e . pytest ruff

      - name: Validate sample packet
        run: |
          python scripts/validate_customer_closeout_handoff.py \
            --packet examples/customer-closeout-handoff/customer-closeout-handoff.sample.json

      - name: Validate live sanitized example
        run: |
          python scripts/validate_customer_closeout_handoff.py \
            --packet examples/customer-closeout-handoff/customer-closeout-handoff.live.sanitized.example.json \
            --require-live

      - name: Test
        run: python -m pytest tests/test_customer_closeout_handoff.py -q

      - name: Lint
        run: |
          python -m ruff check \
            src/cavra/customer_closeout_handoff.py \
            scripts/validate_customer_closeout_handoff.py \
            tests/test_customer_closeout_handoff.py
