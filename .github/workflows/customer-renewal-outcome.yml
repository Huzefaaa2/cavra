name: Customer Renewal Outcome

on:
  pull_request:
    paths:
      - "src/cavra/customer_renewal_outcome.py"
      - "scripts/validate_customer_renewal_outcome.py"
      - "examples/customer-renewal-outcome/**"
      - "tests/test_customer_renewal_outcome.py"
      - ".github/workflows/customer-renewal-outcome.yml"
  push:
    branches:
      - main
    paths:
      - "src/cavra/customer_renewal_outcome.py"
      - "scripts/validate_customer_renewal_outcome.py"
      - "examples/customer-renewal-outcome/**"
      - "tests/test_customer_renewal_outcome.py"
      - ".github/workflows/customer-renewal-outcome.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CAVRA
        run: python -m pip install -e . pytest ruff

      - name: Validate sample packet
        run: |
          python scripts/validate_customer_renewal_outcome.py \
            --packet examples/customer-renewal-outcome/customer-renewal-outcome.sample.json

      - name: Validate live sanitized example
        run: |
          python scripts/validate_customer_renewal_outcome.py \
            --packet examples/customer-renewal-outcome/customer-renewal-outcome.live.sanitized.example.json \
            --require-live

      - name: Test
        run: python -m pytest tests/test_customer_renewal_outcome.py -q

      - name: Lint
        run: |
          python -m ruff check \
            src/cavra/customer_renewal_outcome.py \
            scripts/validate_customer_renewal_outcome.py \
            tests/test_customer_renewal_outcome.py
