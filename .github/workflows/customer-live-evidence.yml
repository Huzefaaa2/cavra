name: Customer Live Evidence

on:
  pull_request:
    paths:
      - "src/cavra/customer_live_evidence.py"
      - "scripts/validate_customer_live_evidence.py"
      - "examples/customer-live-evidence/**"
      - "tests/test_customer_live_evidence.py"
      - ".github/workflows/customer-live-evidence.yml"
  push:
    branches:
      - main
    paths:
      - "src/cavra/customer_live_evidence.py"
      - "scripts/validate_customer_live_evidence.py"
      - "examples/customer-live-evidence/**"
      - "tests/test_customer_live_evidence.py"
      - ".github/workflows/customer-live-evidence.yml"

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CAVRA
        run: python -m pip install -e . pytest ruff

      - name: Validate sample packet
        run: |
          python scripts/validate_customer_live_evidence.py \
            --packet examples/customer-live-evidence/customer-live-evidence.sample.json

      - name: Validate live sanitized example
        run: |
          python scripts/validate_customer_live_evidence.py \
            --packet examples/customer-live-evidence/customer-live-evidence.live.sanitized.example.json \
            --require-live

      - name: Test
        run: python -m pytest tests/test_customer_live_evidence.py -q

      - name: Lint
        run: |
          python -m ruff check \
            src/cavra/customer_live_evidence.py \
            scripts/validate_customer_live_evidence.py \
            tests/test_customer_live_evidence.py
