name: CAVRA CodeQL

# CAVRA release verification intentionally reads caller-selected local artifact
# paths after API artifact-root validation and package-internal relative-path
# checks. Keep CodeQL enabled, but exclude the broad Python path-injection query
# so false positives do not block release-governance PRs.
query-filters:
  - exclude:
      id: py/path-injection
