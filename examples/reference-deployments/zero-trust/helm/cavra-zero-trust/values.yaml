image:
  repository: cavra/community-api
  tag: zero-trust-reference
  pullPolicy: IfNotPresent

tenant:
  id: trial-tenant-reference
  workspaceId: zero-trust-lab

runtime:
  failClosed: true
  corsOrigins: "https://sandbox.example.invalid"

scanner:
  enabled: true
  mode: metadata_only
  noRawModelEgress: true
  image: python:3.12-slim
  evidencePath: /data/zero-trust-reference-scan.json

service:
  port: 8000
