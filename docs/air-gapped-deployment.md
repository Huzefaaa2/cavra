# Air-Gapped Deployment

Air-gapped deployment should package the CLI, policy packs, schemas, MCP server, API container, immutable local evidence storage, and optional Go enforcement binary. External integrations should be disabled or routed through approved internal endpoints.
