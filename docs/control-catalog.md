# Control Catalog

Controls: secret file read block, sensitive write approval, Terraform/OpenTofu apply block, Kubernetes destructive command block, cloud IAM expansion block, protected branch direct push block, unknown MCP server block, PR attestation generation, session audit, SIEM export, and approval routing.
