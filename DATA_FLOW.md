# Data Flow

Agent action request -> CAVRA runtime guard -> policy registry -> decision response -> audit/evidence writer -> optional PR attestation, SIEM export, approval workflow, or sandbox display.
